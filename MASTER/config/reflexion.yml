reflexion:
  decay_settings:
    days: 30
    factor: 0.4
  confidence_thresholds:
    threshold: 0.6
  max_retries: 3
  react_max_steps: 15
  dream_frequencies: []  # Add frequencies as needed
  model_selections:
    - smart
    - cheap
    - creative
  memory_limits: {}  # Define memory limits
  tool_configurations: {}  # Configure tools as necessary
